func int fact (int n)
 {
 	int x;
 	if (n == 0)
 	{
 		return 1;
 	}
 	return n * fact(n - 1);
 }

 main 
 {
 	int i = 10;
	int x = i;
 	read(i);
 	x = x + 15;
 	while (i >= 1)
 	{
 		write(i);
 		i = i - 1;
 	}
 	read(i);
 	write(i);
 }